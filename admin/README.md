# Travel GTR Admin Panel

Админ-панель для управления сайтом travel.gtr.lv

## 🚀 Доступ к админке

URL: `https://travel.gtr.lv/admin/`

**Важно:** Измените пароль по умолчанию!

### Первоначальная настройка:

1. Откройте `admin/config.php`
2. Измените `ADMIN_USERNAME` и `ADMIN_PASSWORD_HASH`
3. Для генерации нового хеша пароля используйте:
   ```php
   echo password_hash('ваш-новый-пароль', PASSWORD_DEFAULT);
   ```

## 📋 Возможности

### 1. Управление меню
- Добавление/редактирование/удаление пунктов меню
- Многоязычные названия (EN, LV, RU)
- Настройка порядка отображения
- Указание URL для каждого пункта

### 2. Управление страницами
- Создание и редактирование страниц
- Многоязычный контент
- Управление SEO метаданными

### 3. Управление переводами
- Централизованное управление всеми переводами
- Поддержка 3 языков: English, Latviešu, Русский
- Добавление новых ключей переводов

### 4. Настройки сайта
- Настройка названия сайта
- Размеры логотипа
- Отступы header
- Цветовая схема
- И другие параметры стиля

## 📁 Структура файлов

```
admin/
├── config.php          # Конфигурация и авторизация
├── index.php           # Страница входа
├── dashboard.php       # Главная панель управления
├── admin.css           # Стили админки
├── admin.js            # JavaScript для управления
├── api/
│   └── save.php        # API для сохранения данных
└── data/               # Данные (создается автоматически)
    ├── menu.json       # Меню сайта
    ├── pages.json      # Страницы
    ├── translations.json # Переводы
    └── settings.json   # Настройки
```

## 🔒 Безопасность

1. **Измените пароль по умолчанию!**
2. Защитите директорию `admin/` через `.htaccess`:
   ```apache
   # Deny access to data directory
   <Directory "admin/data">
       Deny from all
   </Directory>
   ```
3. Регулярно делайте резервные копии файлов в `admin/data/`

## 📝 Использование

### Управление меню:

1. Зайдите в админку
2. Перейдите в "Menu Management"
3. Нажмите "Add Menu Item" для добавления нового пункта
4. Заполните названия на всех языках
5. Укажите URL
6. Нажмите "Save"

### Управление переводами:

1. Перейдите в "Translations"
2. Выберите язык (EN/LV/RU)
3. Отредактируйте существующие переводы или добавьте новые
4. Изменения сохраняются автоматически

### Настройки сайта:

1. Перейдите в "Settings"
2. Измените нужные параметры
3. Нажмите "Save Settings"
4. Изменения применятся к сайту

## 🔄 Интеграция с сайтом

Данные из админки автоматически загружаются на сайт через:
- `load-admin-data.php` - генерирует JavaScript с данными
- Данные доступны в `window.AdminData`

Для использования на сайте добавьте в `index.html`:
```html
<script src="load-admin-data.php"></script>
```

## ⚠️ Важно

- Все данные хранятся в JSON файлах
- Делайте резервные копии директории `admin/data/`
- При изменении настроек может потребоваться обновление CSS на сайте
- Изменения применяются сразу после сохранения

## 🐛 Решение проблем

**Не могу войти в админку:**
- Проверьте логин и пароль в `config.php`
- Убедитесь, что сессии работают

**Данные не сохраняются:**
- Проверьте права доступа к `admin/data/`
- Убедитесь, что директория существует и доступна для записи

**Изменения не отображаются на сайте:**
- Проверьте, что `load-admin-data.php` подключен
- Очистите кеш браузера

## 📞 Поддержка

При возникновении проблем проверьте:
1. Права доступа к файлам
2. Логи ошибок PHP
3. Консоль браузера (F12)

